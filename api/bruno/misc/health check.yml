info:
  name: health check
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/v2/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
