info:
  name: version check
  type: http
  seq: 5

http:
  method: GET
  url: "{{API_URL}}/v2/version"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
