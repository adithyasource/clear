info:
  name: get multiple assets
  type: http
  seq: 10

http:
  method: GET
  url: "{{API_URL}}/games/assets/5209422"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
