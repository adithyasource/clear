info:
  name: search game
  type: http
  seq: 1

http:
  method: GET
  url: "{{API_URL}}/v2/games/search/cyberpunk 2077"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
