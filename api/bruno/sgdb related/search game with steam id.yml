info:
  name: search game with steam id
  type: http
  seq: 2

http:
  method: GET
  url: "{{API_URL}}/v2/games/steam/409710"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
