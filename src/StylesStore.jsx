import { createWritable } from "solid-js/store";

export const styles = createWritable({
  borderRadius: 0,
  secondaryColor: "#ffffff",
  secondaryColorForBlur: "#cccccc",
  primaryColor: "#000000",
  modalBackground: "#00000080",
});
